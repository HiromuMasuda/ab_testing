require 'spec_helper'

describe AbTesting do
  it 'has a version number' do
    expect(AbTesting::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
